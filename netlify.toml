[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-sitemap"
  [plugins.inputs]
    baseUrl = "https://nomadneuro.com"
    exclude = [
      "/404.html",
      "/signin",
      "/signin.html"
    ]
